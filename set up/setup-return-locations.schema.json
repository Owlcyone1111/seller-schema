{
    "title": "SetupReturnLocations",
    "type": "object",
    "additionalProperties" : false,
    "properties": {
        "returns_locations":{
            "type":"array",
            "required":true,
            "minItems":1,
            "description":"The locations where you accept returns",
            "items":{
                "$ref":"#/definitions/return_location_definition"
            }
        }
    },
    "definitions": {
        "return_location_definition": {
            "title": "ReturnsLocationDefinition",
            "type": "object",
            "properties": {
                "returns_location_id": {
                    "type": "string",
                    "description": "The Jet-assigned unique ID of this returns locations.  This only needs to be provided if you are updating a return location"
                },
                "address_id": {
                    "type": "string",
                    "description": "The Jet-assigned unique ID of of the address assigned to this location

                    #Logic
                    *Either the address_id or the alt_address_id are required
                    "
                }
            }
        }
    }
}