{
    "title" : "RegistrationMerchantCreated",
    "type" : "object",
    "additionalProperties" : false,
    "properties" : {
    	"merchant_id": {
            "type": "string",
            "required":true,
            "description" :  "The merchant_id (Jet's 32 character unique identifier) assigned to the newly created merchant"
        },
        "address_id" : {
            "type":"string",
            "required":true,
            "description":"The address_id (Jet's 32 character unique identifier) assigned to the newly created address"
        },
        "is_vip" : {
            "type":"boolean",
            "required":true,
            "description":"If true, the merchant created is a VIP"
        }
    }
}