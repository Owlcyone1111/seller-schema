{
	"title": "Image",
	"type": "object",
	"description": "This schema is used to set images for merchant skus",
	"properties": {
		"merchantSku": {
			"type": "string",
			"description": "Your unique identifier for each product you sell on Jet"
		},
		"imageType": {
			"type": "string",
			"description": "Whether it is the \"Main Image\" ,\"Alternate Image\" or  a  \"Swatch Image\" for the given SKU that is being added"
		},
		"imageUrl": {
			"type": "string",
			"description": "URL location where Jet.com can access the image"
		},
	}
}